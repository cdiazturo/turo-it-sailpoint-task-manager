# Breaking changes in v4

- Set `strict: true` in tsconfig.json for additional strict rule enforcement (e.g. `noUncheckedIndexedAccess`).
- Set `noUncheckedIndexedAccess: true` in tsconfig.json for enhanced type standards around indexing/property access.
